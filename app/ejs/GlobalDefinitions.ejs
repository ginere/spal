<% // -*- javascript -*-

render={};

render.pub=function(name){	
	var html;

	if (DEBUG) {
		html=include(config.PATH+'/ejs/pub/'+name+'.debug');
	} else {
		html=include(config.PATH+'/ejs/pub/'+name);
	}

//	return htmlclean(html);
	return html;
};


// This load the tamplates from the /ejs/templates/ Folter
render.template=function(name){
	// console.error("PATH:"+config.PATH+", NAME:"+name);
	var html=include(config.PATH+'/ejs/templates/'+name);

	// TODO
	//return htmlclean(html);
	return html;
};

render.pageForTemplate=function(name){
	var html=include(config.PATH+'/ejs/'+name);

	// TODO
	//return htmlclean(html);
	return html;
};

render.templateJs=function(name){
	var html=render.template(name);
	html=html.replace(/"/g, '\\"');
	html=html.replace(/'/g, "\\'");

	return html;
};


%>